<%= form_for(@focusing_section) do |f| %>
  <% if @focusing_section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@focusing_section.errors.count, "error") %> prohibited this focusing_section from being saved:</h2>

      <ul>
      <% @focusing_section.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :industry_id %><br />
    <%= f.text_field :industry_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
